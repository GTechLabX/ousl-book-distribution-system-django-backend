from django.dispatch import Signal

district_add_requested = Signal()
district_update_requested = Signal()
district_delete_requested = Signal()
district_show_requested = Signal()
district_all_show_requested = Signal()
